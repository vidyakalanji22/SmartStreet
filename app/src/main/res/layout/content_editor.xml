<?xml version="1.0" encoding="utf-8"?>

<!-- Main Relative Layout-->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="#EEEEEE"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="msd.com.smartstreet.EditorActivity"
    tools:showIn="@layout/activity_editor">

    <!-- EditText field to enter new comment -->
    <EditText
        android:layout_width="match_parent"
        android:layout_height="350dp"
        android:id="@+id/editText"
        android:gravity="top"
        android:textColor="#000000"
        android:layout_alignParentStart="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentTop="true" />

    <!-- Rating bar to enter the rating -->
    <RatingBar
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/editText"
        android:paddingLeft="5dp"
        android:id="@+id/ratingBarEditor"
        android:layout_centerInParent="true"
         />

    <!-- Save Button -->
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Save"
        android:background="@drawable/button"
        android:layout_below="@id/ratingBarEditor"
        android:layout_centerInParent="true"
        android:id="@+id/saveButton"/>
</RelativeLayout>
