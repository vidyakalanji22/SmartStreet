<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#EEEEEE"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="msd.com.smartstreet.BarcodeActivity"
    tools:showIn="@layout/activity_barcode">

    <!-- This ImageView is for the image at the top of the barcode activity page -->
    <ImageView
        android:id="@+id/imageView"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_alignParentTop="true"
        android:layout_gravity="center"
        android:src="@drawable/ic_sstreet" />
    <!-- This TextView represents the text below the image about what smart street can do.
    The text is places in strings.xml-->
    <TextView
        android:id="@+id/longText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_margin="5dp"
        android:text="@string/smartstreet"
        android:textColor="#2F4F4F"
        android:textSize="20dp"
        />

    <!-- This ImageButton is for the Scan button -->
    <ImageButton
        android:layout_gravity="center"
        android:id="@+id/scanButton"
        android:layout_width="wrap_content"
        android:layout_height="80dp"
        android:background="#00F0F8FF"
        android:src="@drawable/ic_scanicon"
        android:layout_marginTop="25dp"/>

    <!-- This TextView is for the Scan button text -->
    <TextView
        android:layout_gravity="center"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Scan"
        android:textColor="#2F4F4F"
        android:textSize="20dp"/>


    <!-- This TextView is to print the information about the scanned item -->
    <TextView
        android:id="@+id/content"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="5dp"
        android:paddingBottom="5dp"
        android:paddingTop="5dp"
        android:textColor="#000000"
        android:textSize="15dp"
        android:autoLink="web"/>



</LinearLayout>
